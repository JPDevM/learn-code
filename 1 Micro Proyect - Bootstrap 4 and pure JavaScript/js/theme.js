'use strict';

// Function to change the theme to Sailor Night
function modifyThemeSailorNight() {
  var bodyThemeColor = document.getElementById('body');
  console.log(bodyThemeColor);
  // var bodyTextThemeColor = document.getElementById("body");
  // var navVarThemeColor = ;
  // var cardThemeColor = ;

  /*
  if (bodyThemeColor.classList.contains('bg-dark'))
    bodyThemeColor.classList.replace('bg-dark', 'bg-transparent');
  else bodyThemeColor.classList.toggle('bg-transparent');

  if (bodyThemeColor.classList.contains('bg-dark'))
    bodyThemeColor.classList.replace('bg-dark', 'bg-transparent');
  else bodyThemeColor.classList.toggle('bg-transparent');

  //hacer las que siguen
*/
}

// Function to change the theme to Sailor Day
function modifyThemeSailorDay() {}
